<nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item text-light">
                    <a class="nav-link active text-light fs-5 fw-bolder" aria-current="page"
                       href="{{ path('app_home') }}">MonSpotify</a>
                </li>
                {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link text-light fs-5" href="{{ path('app_artist_index') }}">Artist</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-light fs-5" href="{{ path('app_music_index') }}">Music</a>
                </li>
            </ul>
            <ul class="navbar-nav d-flex flex-row-reverse w-100">
                <li class="nav-item">
                    <a type="button" class="btn btn-danger" href="{{ path('app_logout') }}">Logout</a>
                </li>
            </ul>
                {% else %}
            <ul class="navbar-nav d-flex flex-row-reverse w-100">
                    <li class="nav-item">
                        <a class="nav-link text-light fs-5" href="{{ path('app_register') }}">Registration</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-light fs-5" href="{{ path('app_login') }}">Login</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>